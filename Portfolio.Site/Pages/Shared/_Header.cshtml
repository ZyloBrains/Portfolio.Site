@using Portfolio.Site.Data
@inject Portfolio.Site.Data.ApplicationDbContext _db

<header class="header header-6">
    <div class="navbar-area">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-12">
                    <nav class="navbar navbar-expand-lg">
                        <a class="navbar-brand" href="index.html">
                            <img src="/img/logo/logo.png" alt="Logo" />
                        </a>
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent6" aria-controls="navbarSupportedContent6" aria-expanded="false" aria-label="Toggle navigation">
                            <span class="toggler-icon"></span>
                            <span class="toggler-icon"></span>
                            <span class="toggler-icon"></span>
                        </button>

                        <div class="collapse navbar-collapse sub-menu-bar" id="navbarSupportedContent6">
                            @{
                                var sections = _db.Sections
                                .Where(s => s.Site.Name == Constants.SiteName)
                                .Where(s => s.Enabled)
                                .OrderBy(s => s.Order)
                                .Select(s => new
                                {
                                    s.Title,
                                    s.Slug
                                })
                                .ToArray();
                            }

                            <ul id="nav6" class="navbar-nav ms-auto">                                
                                @for (int i = 0; i < sections.Length; i++)
                                {
                                    <li class="nav-item">
                                        <a class="page-scroll @(i == 0 ? "active" : "")" href="#@sections[i].Slug">@sections[i].Title</a>
                                    </li>
                                }
                            </ul>
                        </div>
                    </nav>
                </div>
            </div>
        </div>
    </div>
</header>
